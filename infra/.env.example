# Copy to infra/.env before `docker compose up`. Never commit the real .env.
# SQL Server SA password — must meet MSSQL complexity rules (>=8 chars, 3 of 4 char classes).
MSSQL_SA_PASSWORD=Your_strong_Passw0rd
# Api environment: Development (Swagger on) or Production.
ASPNETCORE_ENVIRONMENT=Development
