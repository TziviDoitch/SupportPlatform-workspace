import { PagePlaceholder } from '../../../components/PagePlaceholder';

export function SearchPage() {
  return (
    <PagePlaceholder
      title="חיפוש"
      description="טופס דינמי מ‑metadata + פאנל שאלה קריאה — נבנה ב‑S3."
    />
  );
}
